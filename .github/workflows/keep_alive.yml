name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Streamlit URL
      run: |
        curl -I "https://universal-scalper-app-ke2f5jekpmxecjgtsdd6zk.streamlit.app/"
